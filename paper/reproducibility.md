# Reproducibility

The PACL Layer 1 prototype is designed to be fully reproducible.

## Requirements

Python environment with packages listed in:

`requirements.txt`

---

## Running the full pipeline

From repository root:

```bash
./run_pacl_layer1.sh
